list(APPEND IN_SOURCE_FILES
    main.cpp
)

foreach(SOURCE_FILE ${IN_SOURCE_FILES})
    list(APPEND PROJECT_SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/${SOURCE_FILE})
endforeach()

set(PROJECT_SOURCE_FILES "${PROJECT_SOURCE_FILES}" PARENT_SCOPE)
